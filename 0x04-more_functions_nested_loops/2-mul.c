#include <unistd.h>
#include "main.h"
/**
 * mul - multiplies 2 numbers
 * @a: 1st operand
 * @b: 2nd opernd
 *
 * Return: returns the mul
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
